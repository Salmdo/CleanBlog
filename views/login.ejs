<!DOCTYPE html>
<html lang="en">
    <%- include('layouts/header') -%>
    <body>
        <%- include('layouts/navbar') -%>
        <!-- Page Header-->
        <header class="masthead" style="background-image: url('assets/img/contact-bg.jpg')">
            <div class="container position-relative px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <div class="page-heading">
                            <h1>Login</h1>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        <!-- Main Content-->
        <!-- Main Content -->
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-md-10 mx-auto">
                <% if (loginError != null && loginError.length > 0) {%>
                <ul class="list-group">
                    <li class="list-group-item list-group-item-danger"><%= loginError %></li>
                </ul>
                <%} %>
                <!-- when the form is submitted, the browser will make a ‘ POST ’ request to the /posts/store endpoin -->
                <!-- enctype="multipart/form-data" is for the browser to know that the form contains multi media
                The browser will then encrypt the multi media before sending it to the server. -->
                <form action="/user/login" method="POST">
                    <div class="control-group">
                        <div class="form-group floating-label-form-group controls">
                            <label>User Name</label>
                            <!-- be passed from the form to the server? => need need to have the name attribute for each field -->
                            <input type="text" class="form-control" placeholder="User Name" id="username" name="username" value="<%= usernamEntered %>" >
                        </div> 
                    </div>
                    <div class="control-group">
                        <div class="form-group floating-label-form-group controls">
                            <label>Password</label>
                            <!-- be passed from the form to the server? => need need to have the name attribute for each field -->
                            <input type="text" class="form-control" placeholder="Password" id="password" name="password" >
                        </div> 
                    </div>
                    <br>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">Login</button>
                    </div>
                </form>
            </div> 
        </div>
        </div>
        <%- include('layouts/footer') -%>
        <%- include('layouts/scripts') -%>
    </body>
</html>
